#include "2-main.h"
#include <stdio.h>

/**
 * _strlen -  returns the length of a string.
 * @s: sequence of char
 * Return: length of string
 */

int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
