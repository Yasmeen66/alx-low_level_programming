#include "3-main.h"
#include <stdio.h>

/**
 * _strcmp - compares two strings.
 * @s1: string variable
 * @s2: string variable
 * Return: int number
 */

int _strcmp(char *s1, char *s2)
{
	int res;

	res = strcmp(s1, s2);

	return (res);
}
